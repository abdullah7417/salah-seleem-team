# Plan: Copy Lifestyle Pricing Layout to Biomedical `new-pricing.tsx`

## Goal
Create a new pricing component at `merged/src/landings/biomedical/sections/new-pricing.tsx` that uses the **same layout, structure, and logic** as the lifestyle `Pricing.jsx` (dropdown-based membership selector), but themed with **biomedical colors**.

## Source Files
- **Layout & Logic**: `merged/src/landings/lifestyle/sections/Pricing.jsx`
- **Theme Reference**: `merged/src/landings/biomedical/sections/Pricing.tsx`
- **Data Source**: `merged/src/landings/biomedical/data/index.js`

## Changes

### 1. Write `merged/src/landings/biomedical/sections/new-pricing.tsx`

**Logic** (identical to lifestyle):
- `FALLBACK_PLANS` built from biomedical data (`pricingOptions`, `pricingFeatures` from `../data`)
- `useLandingData()` for API-driven plans
- `useState<Record<string, string | number | null>>({})` for selections
- `useEffect` to set default selections (prefer `most_popular` membership)
- `getSelectedMembership`, `handleSelect`, `handlePay` callbacks
- Payment redirect to `https://salahseleemteam.com/membership/membership/payment/plans/{planId}/{membershipId}`
- `validPlans` filter and `isGrid` logic
- `getFeatures` callback with fallback to static features

**Layout** (same structure as lifestyle `PlanCard`):
- Dropdown `<select>` for membership duration (not radio buttons)
- Price display with before/discount
- Description text
- Pay button
- Features list with checkmarks
- "Most Popular" badge
- `motion.div` + `AnimatePresence` wrapper
- Grid layout for multiple plans, single column for one plan

**Theme** (biomedical colors replacing lifestyle orange):
| Lifestyle | Biomedical Replacement |
|---|---|
| `bg-[#f8a53d]` (orange card) | `bg-white border border-[#E5E7EB]` (white card) |
| `bg-[#fdf6ef]` (select bg) | `bg-brand-offwhite` (select bg) |
| `text-primary` (orange text) | `text-brand-red` |
| `bg-black` (CTA button) | `bg-brand-red hover:bg-brand-red-hover` |
| `bg-[#22c55e]` (checkmark circle) | `bg-brand-teal` |
| `text-black` | `text-text-primary` |
| Section `bg-background` | `bg-brand-offwhite` |
| `focus:ring-primary/40` | `focus:ring-brand-red/40` |
| `text-white` on checkmark | `text-white` (same) |

**TypeScript**: Type all props and state (follow biomedical `Pricing.tsx` patterns with `any` for API data).

### 2. No other files need modification

The file `new-pricing.tsx` is currently empty and not imported in `sections.js` yet. The user likely just wants the component created and will integrate it separately.
