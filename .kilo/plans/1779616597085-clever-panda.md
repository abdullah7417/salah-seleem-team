# Plan: Add "Most Popular" Badge & Styling to Biomedical Pricing

## File
`merged/src/landings/biomedical/sections/Pricing.tsx`

## Changes

### 1. Fix radio button badge text (line 165)
The existing badge on the membership radio buttons references `apiData?.membership_plans?.[0]?.most_popular_title` but `apiData` is **not in scope** inside `PlanCard`. Fix it to use `plan.most_popular_title`:

```tsx
// Before (line 165):
{apiData?.membership_plans?.[0]?.most_popular_title || "الأكثر شعبية"}

// After:
{plan.most_popular_title || "الأكثر شعبية"}
```

### 2. Add badge ribbon + highlighted border on main card div (line 202)
The main card div currently:
```tsx
<div className="bg-white overflow-hidden border-[0.5px] border-[#E5E7EB] border-t-[3px] border-t-brand-red rounded-b-[14px]">
```

**When `selectedMembership.id === plan.most_popular`:**
- Add `relative` positioning (for the absolute badge)
- Change border to `border-2 border-brand-red` for highlight
- Insert a badge `<span>` as the first child:
  ```tsx
  <span className="absolute top-3 left-4 text-[11px] bold text-white bg-brand-red px-3 py-1 rounded-full z-10">
    {plan.most_popular_title || "الأكثر شعبية"}
  </span>
  ```

**Otherwise**, keep the current border styling. Add `relative` to all cards (harmless).

### Implementation approach
- Compute `isMostPopular = Number(m?.id) === Number(plan.most_popular)` inside `PlanCard`
- Use `cn()` to conditionally apply border classes on the card div
- Add the badge span as first child of the card div when `isMostPopular` is true
