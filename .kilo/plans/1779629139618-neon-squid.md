# Plan: Fix Hero Video Behavior — Desktop Inline + Mobile Background

## Problem

After the initial implementation, the Hero video behavior needs adjustment:

1. **Desktop**: Currently opens a `VideoPlayerModal` (lightbox). Should instead play **inline** in the right-column card (replace thumbnail with inline iframe/video player).
2. **Mobile/Tablet (<1024px)**: Currently shows a thumbnail card + modal. Should instead play video as a **muted autoplay background** (the original behavior before the refactor).

## File to Modify

Only `merged/src/landings/biomedical/sections/Hero.tsx` needs changes.

No changes to `videoUtils.ts`, `VideoPlayButton.tsx`, or `VideoPlayerModal.tsx`.

## Detailed Changes to Hero.tsx

### 1. Add `desktopVideoPlaying` state

```tsx
const [desktopVideoPlaying, setDesktopVideoPlaying] = useState(false);
```

### 2. Mobile/Tablet (<1024px) — Background Autoplay

**Remove** the mobile thumbnail card with play button (lines 241-273 in current file).
**Remove** the thumbnail-based background approach in `renderMobileVideoBackground`.
**Restore** background iframe/video behavior for mobile:

- **YouTube**: Render `<iframe>` with `autoplay=1&mute=1&loop=1&controls=0` background embed (same as original)
- **Vimeo**: Render `<iframe>` with `autoplay=1&muted=1&loop=1&controls=0&background=1` background embed
- **MP4**: Render `<video autoPlay muted loop playsInline>` as background
- **No video**: No background, just gradient overlay

The secondary CTA button ("شوف ازاي هتتحول في 90 يوم") will open the `VideoPlayerModal` (for a proper fullscreen viewing experience).

### 3. Desktop (≥1024px) — Inline Play

**Remove** modal open on play button click.
**Add** inline play toggle:

- **Initial state**: Show thumbnail + `VideoPlayButton` overlay in the right column card
- **On click**: Set `desktopVideoPlaying = true`
- **Playing state**:
  - **YouTube/Vimeo**: Render `<iframe>` with `autoplay=1` replacing the thumbnail
  - **MP4**: Render `<video autoPlay controls>` replacing the thumbnail
- **No modal** involved — video stays in its card position

### 4. Keep VideoPlayerModal

The `VideoPlayerModal` is kept for the mobile secondary CTA button — giving users the option to watch the video in fullscreen. It remains available but is NOT used for desktop play.

## Behavior Summary

| Screen | Video Display | Play Trigger | Playback Mode |
|--------|--------------|--------------|---------------|
| Desktop (≥1024px) | Right-column card | Click play button | **Inline** iframe/video replaces thumbnail |
| Mobile/Tablet (<1024px) | Full background | Autoplay on load | **Muted background** iframe/video loop |
| Mobile CTA button | — | Click "شوف ازاي هتتحول في 90 يوم" | **Modal** fullscreen player |

## Implementation Steps

1. Add `desktopVideoPlaying` state
2. Refactor `renderMobileVideoBackground()` to always use background embed (iframe/video) instead of thumbnail
3. Remove the mobile thumbnail card section entirely
4. Refactor desktop right-column card: thumbnail → click → inline iframe/video (no modal)
5. Keep `VideoPlayerModal` only for mobile secondary CTA button
6. Keep mobile CTA button as `<button>` that opens `VideoPlayerModal`
